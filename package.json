{
  "name": "floreria-duhau",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "firebase": "^10.12.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
